global class OrderCsvScheduler implements Schedulable {
    global void execute(SchedulableContext sc) {
        // Implement logic to fetch the necessary data or construct the JSON string
        //String jsonString = '{"OrderNumber": "123", "Id": "recordId"}'; // Replace with your actual JSON data
        
        // Invoke the method from the provided class
        //HandleOrderCsv.InsertinOrder(jsonString);
    }
}